module.exports = {
    websocket_watching: 4206 || process.env.PORT, // Used to monitor the viewers watching the stream.
    rtmp: 1935, // Used for streams.
    http: 2496, // idk maybe for shit users?
    client_webserver: 6900, // the stream client itself?
    https_port: 8443 // will do this later.
}